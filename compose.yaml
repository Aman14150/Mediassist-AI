services:
  website:
    build: .
    ports:
      - "8080:80"
    env_file:
      - .env
    environment:
      DB_CONNECTION: sqlite
    volumes:
      - booking_data:/var/www/html/storage

volumes:
  booking_data:
